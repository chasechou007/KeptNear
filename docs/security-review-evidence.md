# Security Review Evidence Register

This register tracks external security review evidence, review-driven fixes,
maintainer-accepted risks, validation, and release decision status for
KeptNear.

Current status: **Experimental pre-release risks accepted; artifact readiness
not yet approved**. No external security review evidence has been attached.
`AR-001` covers a signed and notarized experimental DMG. `AR-002` covers a
separate unsigned experimental DMG profile. Neither decision recommends
production use or proves that a release artifact passed its applicable gates.

## Review Summary

- Review status: Not started
- Reviewer or firm: None selected yet
- Review window: Not scheduled
- Reviewed commit or release artifact: None selected yet
- Final report: None attached yet
- Finding tracker: None attached yet
- Release decision: Experimental pre-release risk accepted

## Reviewer Reports

| Date | Reviewer | Scope | Artifact | Notes |
| --- | --- | --- | --- | --- |
| None | None selected yet | None reviewed yet | None attached yet | No external report attached yet. |

## Findings

Each finding should link to a reviewer report section, issue, pull request, or
accepted-risk entry.

| ID | Severity | Status | Affected Area | Evidence | Resolution |
| --- | --- | --- | --- | --- | --- |
| None recorded yet | None | Not Applicable | None reviewed yet | None attached yet | No external findings recorded yet. |

Status values:

- Open
- Fixed
- Accepted Risk
- Duplicate
- Not Applicable

## Accepted Risks

Accepted risks are release decisions. Each accepted risk must be explicit enough
for a reviewer or maintainer to re-evaluate later.

| ID | Severity | Affected Behavior | Rationale | User-Facing Implication | Mitigation | Owner | Revisit Trigger |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AR-001 | High | Publishing an externally unaudited signed password-manager binary | KeptNear is a personal-interest, local-first project and the signed binary is explicitly limited to an experimental pre-alpha for informed testers. External review is deferred, not treated as completed. | Unknown security defects could expose vault secrets or damage vault data. Testers must not store production credentials. | Require a clean source commit, Apple Silicon-only build checks, Developer ID signing, hardened runtime, Apple notarization, DMG and app verification, published checksum and manifest, experimental warnings, manual updates, and no automatic telemetry or hosted vault service. | Chase Chou | Revisit before recommending production use, removing the experimental warning, broadening platform or audience scope, declaring 1.0, or after a material cryptographic or vault-format change. |
| AR-002 | High | Publishing an externally unaudited and unsigned password-manager DMG | Developer enrollment may not be available for the first personal-interest preview. The unsigned profile is limited to an experimental Apple Silicon pre-alpha for informed testers and makes no Apple trust-chain claim. | macOS will warn during installation, identity cannot be verified through Developer ID, and unknown security defects could expose vault secrets or damage vault data. Testers must verify the published checksum and must not store production credentials. | Require a clean source commit, Apple Silicon-only build checks, dedicated unsigned release mode and verifier, DMG and app integrity checks, published checksum and manifest, adjacent unsigned/unaudited/experimental warnings, explicit installation guidance, manual updates, and no automatic telemetry or hosted vault service. | Chase Chou | Revisit when Developer ID signing becomes available; before recommending production use, removing any warning, broadening platform or audience scope, declaring 1.0, or after a material cryptographic or vault-format change. |

Required accepted-risk fields:

- severity
- affected behavior
- reason for acceptance
- user-facing implication
- mitigation or compensating control
- owner
- revisit trigger

## Review-Driven Fixes

| Finding ID | Change | Validation | Status |
| --- | --- | --- | --- |
| None recorded yet | None | None | No review-driven fixes recorded yet. |

## Validation Evidence

Record validation after review-driven fixes or accepted-risk decisions. Local
pre-review baseline validation may be recorded here, but it does not complete
external review and must be repeated if reviewer findings drive changes.

| Date | Command Or Check | Result | Notes |
| --- | --- | --- | --- |
| 2026-06-29 | `scripts/check.sh` | Passed | Pre-review local baseline; passed with Rust checks, FFI build, Swift workflow tests, and Swift build. Repeat after review-driven fixes. |
| 2026-06-29 | `script/package_macos_alpha.sh` | Passed | Generated unsigned local alpha archive; signing and notarization are not covered by this evidence. |
| 2026-06-29 | `script/verify_macos_alpha_artifact.sh` | Passed | Verified unsigned alpha archive checksum, manifest, bundle contents, manual update channel, and `.pswvault` metadata. |
| 2026-06-29 | `script/package_security_review_materials.sh` | Passed | Generated security review handoff archive with manifest and SHA-256 checksum; this is not evidence that external review is complete. |
| 2026-07-24 | `scripts/check.sh` | Passed | Passed public-source checks, 87 Rust tests, 171 Swift tests, dependency-license verification, FFI build, and macOS build after the `AR-001` policy and release-mode changes. |
| 2026-07-24 | `script/package_macos_alpha.sh` | Passed | Generated an unsigned local macOS 13+ Apple Silicon DMG in `local-test` mode; Developer ID signing and notarization remain separate release gates. |
| 2026-07-24 | `script/verify_macos_alpha_artifact.sh` | Passed | Verified DMG integrity, checksum, mounted app structure, Applications link, manifest, arm64-only app and FFI binaries, and non-distributable local-test status. |
| 2026-07-24 | `script/verify_public_source_tree.sh` | Passed | Verified that local AI context, vaults, plaintext exports, credentials, DMGs, and other build products are not publishable source candidates. |
| 2026-07-29 | `scripts/check.sh` and local quality gates | Passed | Local task 3.4 evidence: 152 Rust/CLI/FFI tests, 197 macOS tests, 152 public-source candidates, 50 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, and frozen-format readiness. This is not an external security review. |
| 2026-07-29 | `scripts/check.sh` and local quality gates | Passed | Local tasks 3.5-3.7 evidence: 153 Rust/CLI/FFI tests, 202 macOS tests, 152 public-source candidates, 50 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and recovery failure-boundary coverage. This is not an external security review. |
| 2026-07-29 | `scripts/check.sh` and local quality gates | Passed | Local task 4.1 evidence: 160 Rust/CLI/FFI/Broker tests, 202 macOS tests, 155 public-source candidates, 52 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and fail-closed device-path coverage. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 4.2 evidence: 170 Rust/CLI/FFI/Broker tests, 202 macOS tests, 157 public-source candidates, 55 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and device-root-key fail-closed plus Keychain policy coverage. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 4.3 evidence: 188 Rust/CLI/FFI/Broker tests, 202 macOS tests, 162 public-source candidates, 71 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, encrypted device-state round trips, and fail-closed key, ciphertext, schema, permission, and plaintext-header checks. SQLCipher 4.5.3 remains a documented pre-distribution upgrade blocker. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 4.4 evidence: 209 Rust/CLI/FFI/Broker tests, 202 macOS tests, 165 public-source candidates, 71 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, strict duplicate-key-free protocol parsing, bounded framing, version negotiation, sanitized response, and transport-independent process-loop coverage. No Unix socket or credential capability is enabled by this evidence. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 4.5 evidence: 218 Rust/CLI/FFI/Broker tests, 202 macOS tests, 166 public-source candidates, 73 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and nine real macOS Unix socket tests covering exact permissions, stale and active endpoints, replacement-safe cleanup, OS peer identity, path-free errors, and framed request exchange. No installed Broker service, Consumer authentication, vault session, authorization, or credential capability is enabled by this evidence. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 4.6 evidence: 231 Rust/CLI/FFI/Broker tests, 202 macOS tests, 167 public-source candidates, 73 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and Broker session coverage for stable vault identity, master and local-material unlock, concurrent and cancelled unlock, fresh session identity, monotonic background auto-lock, bounded event overflow, explicit shutdown, process sharing, and path/secret-redacted errors. The lifecycle is internal; no installed Broker service, Consumer authentication, protocol-exposed vault control, authorization, or credential capability is enabled by this evidence. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 4.7 evidence: 236 Rust/CLI/FFI/Broker tests, 202 macOS tests, 168 public-source candidates, 73 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and transactional grant-invalidation coverage for exact vault sessions, retryable lock-event checkpoints, concurrent queue additions, fail-closed overflow, sourced and Allow Once grants, Consumer removal, exact field deletion, and device-reset preparation. Use Grant issuance and protocol authorization remain unavailable. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 4.8 evidence: 241 Rust/CLI/FFI/Broker tests, 202 macOS tests, 169 public-source candidates, 73 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and global machine-access gate coverage for encrypted persistence, restart, idempotence, write failure, concurrent pause linearization, stable `broker-paused` mapping, wall-clock rollback, retained rules and grants, and unaffected human unlock sessions. No Apps & Tools UI, Consumer authentication, grant issuance, or machine credential handler is enabled by this evidence. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 4.9 evidence: 254 Rust/CLI/FFI/Broker tests, 202 macOS tests, 170 public-source candidates, 73 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and reinstall/clear coverage for load-existing-only recovery, missing-key preservation, authenticated reset ordering, corrupt-state recovery, idempotence, symlink and permission rejection, path mismatch, verified Keychain deletion behavior, partial-failure retry, and portable-vault preservation. The macOS confirmation UI, installed Broker lifecycle, live destructive Keychain integration test, and future Consumer private-key inventory remain unimplemented. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 4.10 evidence: 261 Rust/CLI/FFI/Broker tests, 202 macOS tests, 172 public-source candidates, 73 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and seven cross-module Broker runtime tests using a real temporary current-format vault plus SQLCipher state. Coverage includes fresh process identity and empty sessions after restart, persisted pause and rules, stale-grant removal after abrupt exit, transactional graceful shutdown, ciphertext corruption, missing and wrong device keys, insecure database permissions, missing database state, no replacement initialization, and path/secret-free failures. No installed listener service, Consumer authentication, grant issuance, or machine credential capability is enabled by this evidence. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.1 evidence: 271 Rust/CLI/FFI/Broker tests, 202 macOS tests, 173 public-source candidates, 85 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, frozen-format readiness, and Consumer-pairing coverage for five-minute monotonic expiry, bounded pending requests, explicit user approval, immutable Consumer identity, deterministic comparison codes and canonical transcripts, strict Ed25519 proof, replay and tamper rejection, cryptographic-identity uniqueness, no implicit rules or grants, runtime restart and shutdown cancellation, and debug/error redaction. The Ed25519 dependency chain uses reviewed GPLv3-compatible permissive alternatives. Pairing protocol messages, Consumer private-key storage, local approval UI, later connection authentication, authorization, and credential capabilities remain unavailable. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.2 evidence: 276 Rust/CLI/FFI/Broker tests, 202 macOS tests, 174 public-source candidates, 85 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, and frozen-format readiness. A real Unix-socket test observes the current process through the macOS audit token, verifies agreement with `getpeereid`, obtains only a path-free executable basename, and accepts the host's ad-hoc or linker signing evidence without requiring a team identifier. Additional coverage rejects mismatched-user evidence, sanitizes spoofable or unbounded display text, completes Ed25519 pairing with no signing evidence, classifies optional signing states, produces short display fingerprints, and verifies that pairing snapshots exclude full keys, full signature digests, labels, and paths. The basename and signing fields are supporting recognition evidence only; the pairing key and immutable Consumer ID remain the identity root. Protocol and macOS approval presentation remain unavailable. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.3 evidence: 279 Rust/CLI/FFI/Broker tests, 202 macOS tests, 174 public-source candidates, 85 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Pairing is exposed through `BrokerRuntime` methods that accept no authorization scope, while the raw pairing manager and Consumer insertion remain crate-internal. Completion explicitly reports an unchanged authorization effect. Coverage proves that pairing and unlock approvals cannot project access targets, a new pairing with the same label and OS evidence does not inherit another Consumer's metadata-search rule or grant, existing authorization remains unchanged, and completing pairing does not make the Broker advertise `credential.search`. Full Access Rule evaluation, grant issuance, connection authentication, protocol pairing messages, and approval UI remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.4 evidence: 287 Rust/CLI/FFI/Broker tests, 202 macOS tests, 175 public-source candidates, 85 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Eight new tests cover explicit approved creation, exact Consumer/field/capability/version matching, all confirmation policies without grant issuance, bounded lifetime boundaries, expired-rule reauthorization, active-rule conflict rejection, unsupported and incompatible capabilities, unknown Consumers, and pause-before-evaluation behavior. Access Rule evaluation returns policy only and does not unlock a vault, reveal metadata or secrets, or create a Use Grant. Protocol exposure, trusted vault-field lookup, grant issuance, connection authentication, and macOS authorization UI remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.5 evidence: 296 Rust/CLI/FFI/Broker tests, 202 macOS tests, 176 public-source candidates, 85 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Nine new tests cover source-less Allow Once grants, confirmed every-use and once-per-unlock-session rules, automatic-while-unlocked issuance, exact Consumer/field/capability/session matching, session-grant reuse and rotation, rule-capped exclusive expiry, invalid windows and field kinds, pause-before-automatic-issuance and consumption, and a two-connection SQLCipher race in which exactly one caller consumes a one-operation grant. Grant authorization remains non-secret and does not execute metadata search, HTTP, or process capabilities. Protocol exposure, trusted vault-field lookup, asynchronous approvals, connection authentication, and macOS authorization UI remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.6 evidence: 303 Rust/CLI/FFI/Broker tests, 202 macOS tests, 177 public-source candidates, 85 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Seven new tests cover exact stable-ID Core lookup, one-Credential and one-field projection, omission of values and unrelated metadata, non-matching template, tag, username, URL, and other-field data, missing or changed field rejection, bounded and redacted queries, redacted result debugging, exact Grant target enforcement, and real current-format vault integration. Authorized search runs only after pause, Grant, and current-session checks; it returns no vault catalog or secret and is not protocol accessible. Human-side matching, asynchronous approvals, connection authentication, and macOS authorization UI remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.7 evidence: 310 Rust/CLI/FFI/Broker tests, 202 macOS tests, 178 public-source candidates, 85 dependency licenses, Clippy with warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Seven new tests cover bounded control- and bidi-safe descriptions, redacted admitted requests and human reviews, a 50-candidate cap with truncation, capability-compatible field filtering, matching against authenticated username text without returning that value, exclusion of Secret Field values and non-active Credentials from matching, exact selected-scope projection, changed-review rejection, pause-before-admission, human review and confirmation after pause, unknown Consumer rejection, no implicit Rule or Grant, and stale unlock-session rejection in a real current-format Vault. Candidate lists are human-control-plane-only and have no protocol or serialization path. Asynchronous IDs, expiry, coalescing, waiting, polling, resumption, connection authentication, and macOS approval UI remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.8 evidence: 320 Rust/CLI/FFI/Broker tests, 202 macOS tests, 179 public-source candidates, 85 dependency licenses, Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Ten new tests cover random stable request IDs, device-keyed exact-request coalescing, a serialized 256-request cap, 15-minute lifetime and five-minute wait bounds, Consumer-scoped status receipts, exact expiry, atomic first-terminal-state semantics, idempotent decisions, unrelated-notification wakeups, concurrent submission and waiting, process-local zeroizing new-Credential descriptions, human-only queue projection, abrupt-restart reconciliation, and a real current-format Vault flow that creates no Rule or Grant. Exact Access and Unlock requests remain resumable; pairing and new-Credential requests are cancelled after process context is lost. Protocol, CLI, MCP, notifications, and macOS approval-queue exposure remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.9 evidence: 325 Rust/CLI/FFI/Broker tests, 202 macOS tests, 180 public-source candidates, 85 dependency licenses, Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Five new tests cover exact Consumer-field revocation across capabilities, Consumer-wide unpairing, global revocation, idempotence, process-local pairing and approval-context cleanup, waiter wakeup, audit/pause/device-state preservation, human-session preservation in a real Vault, and grant-use versus revocation linearization. Revocation prevents future deliveries but cannot recall secrets already delivered to external processes. Protocol, CLI, MCP, and macOS revocation UI remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 5.10 evidence: 329 Rust/CLI/FFI/Broker tests, 202 macOS tests, 180 public-source candidates, 85 dependency licenses, Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Four new tests plus a strengthened real-Vault search regression cover every known Secret Field kind against field-scoped capabilities and future versions, copied Consumer labels and OS evidence across rule, Grant, and approval boundaries, pairing-proof and one-operation Grant replay, stale Grants across unlock-session and Broker-process identities, and indistinguishable denial for real unrelated, random, and other-Vault metadata probes. Automatic issuance and Grant use now perform non-mutating exact authorization preflight before requested vault-session lookup, preventing an open-Vault identity oracle while preserving final atomic Grant consumption. Protocol connection authentication and externally reachable credential capabilities remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 6.1 evidence: 333 Rust/CLI/FFI/Broker tests, 202 macOS tests, 180 public-source candidates, 85 dependency licenses, Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Audit insertion and retention-setting changes now apply age and 10,000-event bounds in one immediate SQLCipher transaction; startup prunes before exposing the runtime. Four new focused tests cover exact age boundaries, immediate pruning after lowering retention, a persisted monotonic watermark across clock rollback, the absolute event ceiling, and startup pruning; the encrypted database/WAL marker test now also covers audit identities and schema text. The ciphertext-corruption integration fixture always changes an authenticated byte rather than occasionally rewriting the same value. Viewing, filtering, explicit clearing UX, non-secret export, operation attribution, and output-marker coverage remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 6.2 evidence: 339 Rust/CLI/FFI/Broker tests, 202 macOS tests, 181 public-source candidates, 85 dependency licenses, Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, and frozen-format readiness. Six new tests cover every typed filter dimension, invalid limits and inconsistent windows/scopes, stable timestamp-plus-event-ID pagination including equal timestamps and concurrent newer inserts, explicit-confirmation filtered clearing with transactional counts, a fixed versioned JSON export projection, redacted export debugging, and a real portable Vault whose metadata and key envelope remain byte-identical after view, export, and clear. Public view and export APIs obtain retention time from the Broker clock rather than an adapter argument. These APIs remain trusted local runtime operations rather than Consumer protocol messages; App presentation and file-destination selection remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 6.3 evidence: 339 Rust/CLI/FFI/Broker tests, 203 macOS tests, 181 public-source candidates, 85 dependency licenses, Clippy with all targets and features plus warnings denied, strict OpenSpec validation, and frozen-format readiness. A strengthened real-Vault Broker regression stores unique credential-title, URL, raw-secret, request-body, command-argument, standard-stream, response-body, executable-name, full-executable-path, and full-Vault-path evidence at the relevant boundaries, then verifies that audit page/debug/export output contains none of it while view, export, and clear preserve portable Vault metadata and key envelopes. The device-state model and SQLCipher schema checks explicitly deny those payload columns. macOS diagnostics now map Core availability to a closed status instead of forwarding free-form component text; a new snapshot-field whitelist and adversarial marker scan cover item data, operation-like payloads, import/export paths, rejected record names, and full local paths. Future outbound operations and Broker/MCP/CLI/App adapter-wide scans remain tasks 6.4 and 6.6. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 6.4 evidence: 342 Rust/CLI/FFI/Broker tests, 203 macOS tests, 182 public-source candidates, 85 dependency licenses, Clippy with all targets and features plus warnings denied, strict OpenSpec validation, and frozen-format readiness. The trusted Broker runtime now admits only Grant-authorized `http.request` and `process.run` operations through an explicit attribution boundary: it writes a fixed-schema `Pending` audit event before returning a non-cloneable opaque authorization, and consumes that authorization to write exactly one `Allowed` or `Failed` result. Denied and globally paused attempts are separately attributed, unsupported capabilities fail before Grant consumption, and raw generic Grant authorization is crate-internal. Regression coverage verifies exact Consumer, field, capability, Use Grant, confirmation method, event order, and outcomes while rejecting credential values, titles, URLs, request or response bodies, command arguments, standard streams, full paths, telemetry endpoints, template URLs, background-network state, and networking imports from the attribution model. No HTTP client, process executor, telemetry, scheduler, or background template downloader is implemented by this task; actual execution remains tasks 8.5 and 8.6. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 6.5 evidence: 342 Rust/CLI/FFI/Broker tests, 203 macOS tests, 183 public-source candidates, 85 dependency licenses, Clippy with all targets and features plus warnings denied, strict OpenSpec validation, frozen-format readiness, and a new six-document privacy-boundary gate. Logging, diagnostics, architecture, security, product, and update documents now distinguish encrypted device-local audit, explicitly copied support diagnostics, the reserved but currently unwritten `~/.keptnear/logs` directory, external encrypted-file synchronization, explicit default-browser delegation, owner-only Unix-socket IPC, manual updates, offline Usage Profile templates, and the future credential-operation network boundary. They state that outbound attribution is implemented while HTTP and process executors are not, and clarify that a user-approved persistent rule may avoid per-use prompts without authorizing telemetry or unrelated background work. Threat non-claims cover compromised hosts, authorized Consumers, child processes, external services, provider-visible encrypted metadata, clipboard sharing, and the fact that audit proves a local decision rather than intent or endpoint safety. `script/verify_privacy_boundary_docs.sh` is part of `scripts/check.sh` and fails when required boundaries disappear or an unqualified blanket no-network claim is added. This is a documentation consistency gate, not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 8.5 evidence: 381 Rust/CLI/FFI/Broker tests, 229 macOS tests, 194 public-source candidates, 116 dependency licenses, workspace Clippy with warnings denied, strict OpenSpec validation, and frozen-format readiness. The internal Broker HTTP executor binds an exact Consumer-owned Usage Profile, Use Grant, current Vault session, Credential, Secret Field, expected kind, and `http.request` v1 capability before placing one secret. Version 1 is HTTPS-only, rejects URL credentials, fragments, framing and hop-by-hop headers, placement-header overrides, and oversized material, follows no redirects, ignores environment proxies, uses a fixed timeout, and returns only numeric status, a bounded exact-echo-redacted body, and truncation state. Third-party HTTP and TLS logging is compiled out; controlled request, credential-header, capture, and response buffers are zeroized on drop. Real-Vault coverage proves pause-before-Profile ordering, cross-Consumer and capability mismatch rejection without Grant consumption or transport use, exact Bearer placement, changed-kind failure after `Pending`, sanitized network failure, and secret-, URL-, body-, and header-free audit export. The service still receives the approved secret, exact redaction cannot detect transformed derivatives, third-party transport allocations are outside controlled zeroization, and no protocol, MCP, CLI, or App adapter can invoke the executor yet. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 8.6 evidence: 392 Rust/CLI/FFI/Broker tests, 229 macOS tests, 195 public-source candidates, 116 dependency licenses, workspace Clippy with warnings denied, 33 strict OpenSpec items, privacy-boundary verification, and frozen-format readiness. The internal Unix `process.run` executor binds the exact Consumer-owned Usage Profile, Use Grant, current Vault session, Credential, Secret Field, expected kind, and `process.run` v1 capability before direct OS process spawn. Bounded absolute dot-segment-free paths, arguments, explicit environment, secret size, output, and timeout are enforced; inherited environment and working directory are removed; no shell, terminal, PTY, named pipe, or plaintext file is inserted. The three supported placements are a validated child-only environment variable, secret-only stdin with an optional newline, and an anonymous pipe remapped to descriptor 3 with an optional numeric or `/dev/fd/3` reference. Non-blocking writes and bounded per-poll reads prevent pipe deadlock and cancellation starvation; stdout and stderr are independently bounded and exact-secret-redacted across chunks. Timeout and live cancellation close writers, kill, and reap the direct child; controlled request, placement, capture, and returned-output buffers are zeroized. Real-process and real-Vault coverage proves all placements, minimal environment, dual-stream redaction, fixed sanitized failures, Profile preflight before Grant consumption, Pending-to-Allowed and Pending-to-Failed attribution, nonzero-exit handling, and audit export without secret, executable, environment, or output material. Common shell-name rejection is a guardrail rather than executable identity verification; an approved child or descendant can retain, transform, or transmit a delivered secret, split it across streams, or survive direct-child termination, so upstream rotation remains necessary and no protocol, MCP, CLI, or App adapter can invoke this executor yet. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 8.7 evidence: 392 Rust/CLI/FFI/Broker tests, 229 macOS tests, 197 public-source candidates, 116 dependency licenses, workspace Clippy with warnings denied, 33 strict OpenSpec items, privacy-boundary verification, a dedicated compatibility-disclosure gate, and frozen-format readiness. English, Simplified Chinese, and Japanese macOS copy now identifies `process.run` as compatibility delivery at command-line Usage Profile setup, pending request review, credential selection, long-term authorization, saved process Profile review, exact field revocation, and Consumer unpairing. The warning states that the selected process and descendants can read, retain, transform, or transmit the credential; KeptNear revocation or unpairing prevents only future delivery; and invalidating an already delivered copy requires rotation at the upstream credential provider. Capability-scoping tests keep the process warning off ordinary HTTP requests, and repository checks require the same boundary in product, security, and OpenSpec documents. The nonzero-process integration fixture now reads the full stdin-delivered secret before returning a nonzero status, separating successful delivery from a correct `InputDeliveryFailed` result when a child closes its channel early. No UI automation was used. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 8.8 evidence: 397 Rust/CLI/FFI/Broker tests, including 227 Broker tests and 15 focused `process.run` tests, 229 macOS tests, 197 public-source candidates, 116 dependency licenses, workspace Clippy with warnings denied, 33 strict OpenSpec items, privacy and compatibility-disclosure gates, and frozen-format readiness. Coverage exercises environment, secret-only stdin, and anonymous descriptor delivery with numeric and `/dev/fd/3` references; rejects an exact secret repeated in the executable, arguments, working directory, environment name, or environment value; rejects placement collisions plus empty, oversized, NUL-containing, and non-UTF-8 environment secrets; and verifies fixed response and error debug projections. Exact echoes are removed at every possible secret chunk split, stdout and stderr are independently capped at 1 MiB with truncation, and neither returned stream contains the exact delivered value. Continuous dual-stream output cannot starve timeout cleanup, live cancellation kills and reaps the direct child, and a regression child proves that a descendant can inherit the secret environment and keep output descriptors open while the Broker still completes after its bounded drain grace with truncated secret-free output. The descendant intentionally survives the direct-child boundary and remains an explicit reason for upstream rotation; no persistent plaintext file is created. The real-Vault nonzero fixture consumes the complete secret before exiting nonzero, so successful delivery is not confused with an early input-channel close. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 9.1 evidence: 411 Rust tests, including 8 MCP adapter, 233 Broker, 4 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 205 public-source candidates, 116 dependency licenses, workspace Clippy with warnings denied, 33 strict OpenSpec items, privacy and compatibility-disclosure gates, and frozen-format readiness. Broker v1 now exposes resumable pairing plus separate 30-second connection authentication with canonical Ed25519 transcripts, consumed challenges, revocation checks, bounded failure rates, stable non-secret audit, and no authorization side effect. The `keptnear-mcp` binary implements bounded newline-delimited stdio initialization for finalized MCP revisions 2025-11-25 and 2025-06-18, keeps stdout JSON-RPC-only, stores one default Consumer seed in the non-synchronizing device-only macOS Data Protection Keychain with no file fallback, and negotiates, pairs, and authenticates without parsing a Vault or receiving the Broker device root. It advertises no credential tools; the long-running installed Broker service, real user-Keychain integration run, credential methods, separate pairing profiles, and host setup remain later work. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 9.2 evidence: 418 Rust tests, including 12 MCP adapter, 236 Broker, 4 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 207 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, privacy and compatibility-disclosure gates, and frozen-format readiness. Broker protocol v1 now strictly encodes, negotiates, dispatches, and decodes `credential.search`, `access.request`, `grant.status`, `grant.revoke`, `http.request`, and `process.run`. Capability messages contain no caller-selected Consumer identity: the dispatcher requires exact capability negotiation and connection authentication, then derives the Consumer from that connection. Grant status and revoke are Consumer-scoped; foreign and absent identities are indistinguishable, and owned expiry is pruned. Private request and response material has redacted Debug projections and controlled buffers are zeroized; binary JSON fields use base64 transport encoding, not additional encryption. Authenticated MCP initialization advertises exactly six closed-schema tools and no `secret.get`; pairing-pending and Broker-unavailable states advertise none. `tools/call` returns matching structured and compatibility text results, while input and Broker failures use fixed `isError` results without reflecting operation input. Access submission returns an approval identity, but dedicated polling, waiting, and resumption remain task 9.3; separate pairing profiles, full MCP cancellation/compatibility/secret-marker coverage, host setup, installed Broker packaging, and real user-Keychain integration remain later work. HTTP services and approved child processes can retain or transform delivered secrets, and exact redaction cannot detect derivatives. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 9.3 evidence: 421 Rust tests, including 15 MCP adapter, 236 Broker, 4 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 207 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, strict OpenSpec validation, privacy and compatibility-disclosure gates, and frozen-format readiness. The existing `access.request` capability and MCP tool now support strict `status`, `resume`, and one-millisecond-through-five-minute `wait` request kinds in addition to exact and human-matched submission, preserving the six-tool catalog and adding no raw-secret method. Protocol-adapter calls use Broker-owned time after the global Apps & Tools pause gate, derive Consumer ownership from the authenticated connection, and return only result kind, stable approval identity, status, expiry, optional resolution time, and applicable coalescing or timeout state. A real paired and authenticated Runtime/Dispatcher exchange proves submission, status, resumption, timed wait, connection-derived ownership, and identical access-denied responses for foreign and random approval IDs. MCP tests cover all five input variants, closed lifecycle output, invalid wait bounds, and secret-free status, resume, and timeout receipts. Approval subjects, authorization targets, new-Credential descriptions, candidate metadata, and Secret Field values remain absent. Separate pairing profiles, broader lifecycle/cancellation/compatibility and adapter-wide secret-marker vectors, host setup, installed Broker packaging, and real user-Keychain integration remain later work. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 9.4 evidence: 426 Rust tests, including 20 MCP adapter, 236 Broker, 4 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 207 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, strict OpenSpec validation, privacy and compatibility-disclosure gates, and frozen-format readiness. `keptnear-mcp` now accepts either no arguments, `--profile <id>`, or `--profile=<id>`. Profile identifiers are one through 64 canonical lowercase ASCII bytes with alphanumeric endpoints and a closed interior character set; invalid, extra, non-UTF-8, path-like, and unbounded arguments fail before Keychain or Broker access without reflecting input. The no-argument and canonical `default` profile preserve the original `default-v1` Keychain account, while each named profile selects a distinct `profile-v1:<canonical-id>` account under the unchanged non-synchronizing device-only service. Shared profiles reuse one signing key and Consumer permission set; distinct profiles create distinct keys and cannot inherit authorization from one another. The profile label is local non-secret configuration and never enters Broker pairing, authentication, capability, audit, or authorization messages. Tests cover canonicalization, invalid inputs, stable same-profile reuse, different-profile key separation, deterministic Keychain account selection, unchanged Data Protection attributes, and strict executable arguments without accessing the real user Keychain. Broader lifecycle/cancellation/compatibility and adapter-wide secret-marker vectors, host setup, installed Broker packaging, verified multi-profile key cleanup, and real user-Keychain integration remain later work. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 9.5 evidence: 435 Rust tests, including 29 MCP adapter, 236 Broker, 4 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 207 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, strict OpenSpec validation, privacy and compatibility-disclosure gates, and frozen-format readiness. The MCP parser now rejects duplicate JSON keys at every depth before dispatch, accepts exactly one MiB and CRLF framing, rejects the next byte while recovering at the following newline, and keeps stdout JSON-RPC-only. Both frozen MCP revisions and an unknown-revision fallback preserve the same six-tool contract, compatible structured-plus-text output, standard `_meta` handling, and no continuation cursor or experimental Tasks capability. Schema regressions compare runtime-required inputs, closed nested shapes, all five access variants, and one successful output for every tool; they fixed `credential.search.query` as consistently optional. Approval waits and process calls cover exact one-millisecond and five-minute bounds plus the 30-second process default, and incompatible Broker capability sets fail closed. Cancellation notifications are silent and reasons are discarded, but the synchronous adapter does not claim to preempt a call already handed to the Broker; bounded operation timeouts remain authoritative. Success and error projections exclude seeded request-side query, description, URL, header, body, argument, environment, metadata, cancellation-reason, duplicate-value, and unknown-field markers. This is the MCP request-reflection gate, not the later cross-adapter real-secret scan; permitted HTTP bodies and child output remain Broker-redacted operation results. Host setup, installed Broker packaging, real user-Keychain integration, and cross-adapter marker coverage remain later work. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 9.6 evidence: 435 Rust tests, including 29 MCP adapter, 236 Broker, 4 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 209 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, strict OpenSpec validation, privacy and compatibility-disclosure gates, and the new MCP setup documentation gate. `docs/mcp-setup.md` reduces Codex, Claude Code, and generic compatible hosts to one provider-neutral local stdio command, bounded arguments, and a non-secret pairing profile. The concrete commands were checked against installed `codex-cli 0.144.6` and Claude Code `2.1.205`; the generic JSON shape explicitly defers container and field names to each host. The guide requires an absolute executable path, includes no credential environment variable, remote URL, or provider-specific authorization mode, and documents independent versus shared profile permissions, local pairing, separate credential authorization, the fixed six-tool no-raw-secret contract, compatibility-delivery risk, troubleshooting, and the fact that removing a host entry does not revoke Broker state or delete its Keychain key. It labels the setup a developer configuration contract because the installed Broker executable and long-running service are still absent; no host configuration, live Keychain identity, or Broker service was created during validation. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 10.1 evidence: 447 Rust tests, including 29 MCP adapter, 236 Broker, 16 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 211 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, privacy and compatibility-disclosure gates, and the MCP setup documentation gate. The new independent `keptnear` binary defines one closed command tree for status, credential search, exact or human-matched access requests, Grant status and revocation, brokered HTTPS requests, and direct child-process execution while preserving the existing `psw doctor` default binary. Machine commands construct the shared typed Broker request model with canonical Vault, Credential, Secret Field, Use Grant, Usage Profile, and session identities; pairing profiles are bounded canonical local configuration and are not Broker authorization identities. HTTP bodies enter by file path rather than command-line bytes, and process execution requires an explicit `--` followed by a validated absolute executable. Unknown, duplicate, non-UTF-8, cross-kind, or malformed inputs fail with fixed non-reflective output; private parser state is Debug-redacted and controlled argument buffers are zeroized. No `get`, `secret.get`, export, shell interpolation, or raw-secret output command exists. Help and version are local; valid machine commands return a fixed unavailable result until task 10.2 connects the CLI to the Broker, so this validation opened no Vault and accessed neither the Broker nor the user Keychain. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 10.2 evidence: 455 Rust tests, including 10 shared client, 21 MCP adapter, 236 Broker, 22 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 214 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, privacy and compatibility-disclosure gates, and the MCP setup documentation gate. A shared first-party `keptnear-client` now owns exact Broker negotiation, pairing, Ed25519 connection authentication, framed requests, and device-only Consumer signing-key access for both public adapters without parsing a Vault or receiving a Broker device root or Vault key. The existing MCP Keychain service and account contract is preserved; CLI identities use a separate stable service and the same bounded profile rules, preventing accidental permission sharing between adapters. `keptnear status` negotiates non-secret Broker status without loading or creating an identity. Other commands pair or authenticate and then dispatch the shared typed Broker request; pending pairing emits only the request ID and human comparison code and performs no capability call. Every success and error uses a fixed versioned JSON envelope, HTTP response bodies and process streams are base64-framed, and explicit HTTP body files must be regular, non-symlinked, at most one MiB, and are opened with `O_NOFOLLOW` on Unix. No direct Vault decryption, caller-selected Consumer identity, raw-secret retrieval, or whole-Vault export was added. Tests used fakes and a no-Broker status invocation and intentionally did not access a live user Keychain, Vault, or installed Broker. Interactive approval waiting, final run and exit behavior, user documentation, App execution, and installed-service packaging remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 10.3 evidence: 459 Rust tests, including 10 shared client, 21 MCP adapter, 236 Broker, 26 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 214 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, privacy and compatibility-disclosure gates, and the MCP setup documentation gate. `keptnear access request` now submits through the existing authenticated `access.request` capability and, by default, sends exactly one Consumer-scoped wait using the protocol's five-minute maximum. It does not implement a separate polling loop or inspect approval storage. One unique valueless `--no-wait` flag returns the submission receipt immediately and sends no wait request; duplicate and assigned flag forms fail before dispatch. Both modes produce one schema-version-one JSON envelope containing only approval identity, status, time boundaries, coalescing, wait, and timeout state. A bounded pending timeout remains `pending` with `timedOut: true`; it is not presented as approval or denial. A transport, Broker, or typed-response failure after successful submission retains only the stable approval identity needed to retry and does not reflect the approval subject, request description, candidate metadata, authorization target, or Secret Field value. Pairing-pending behavior remains separate and dispatches no access request. Tests cover default terminal wait, no-wait dispatch count, timeout, post-submission transport failure, fixed error output, parser flag uniqueness, and the full existing Broker, MCP, Core, FFI, and macOS suites without accessing a live user Keychain, Vault, or installed Broker. Terminal cancellation, final process compatibility and exit behavior, cross-adapter secret-marker coverage, human and Agent documentation, App execution, and installed-service packaging remain later tasks. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 10.4 evidence: 460 Rust tests, including 10 shared client, 21 MCP adapter, 236 Broker, 27 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 214 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, privacy, compatibility-disclosure, MCP setup, and public-source gates. `keptnear run` remains a thin authenticated adapter over the shared typed `process.run` request: it supplies stable authorization identities, a Usage Profile, explicit caller-owned non-secret process context, and one validated absolute executable, while the Broker alone resolves and places the selected field. CLI help now states that no credential is expanded into the executable, arguments, or KeptNear standard output; that the child or descendants may read, retain, transform, or transmit a delivered value; that local revocation or unpairing prevents future delivery only; and that invalidation requires rotation at the credential provider. Completed runs preserve one schema-version-one JSON envelope, return bounded Broker-redacted stdout and stderr only as base64 fields, and include a fixed secret-free `compatibilityDelivery` object with the same retention, revocation, and rotation boundary. A focused fake-Broker regression proves one `process.run` dispatch, no raw child-stream bytes in CLI output, exact base64 and truncation projection, and a closed eight-field process result. The compatibility gate now covers the CLI help and machine projection in addition to macOS and documentation surfaces. Base64 remains reversible encoding, transformed child output can remain sensitive, and terminal cancellation, exit-code mapping, cross-adapter real-secret scanning, user documentation, App execution, and installed-service packaging remain later tasks. Validation used no UI automation and intentionally accessed no live user Keychain, user Vault, or installed Broker. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` and local quality gates | Passed | Local task 10.5 evidence: 462 Rust tests, including 10 shared client, 21 MCP adapter, 236 Broker, 29 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 214 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, privacy, compatibility-disclosure, MCP setup, and public-source gates. The public CLI remains a closed seven-command tree and provides no raw-value retrieval, reveal, copy, print, dump, or whole-Vault plaintext export command. Known aliases and equivalent raw-output options fail before identity loading or Broker dispatch with the fixed non-reflective `invalid KeptNear command arguments` error. All supported shell interpreter launcher basenames are rejected for `run`; placeholder, command-substitution, backtick, variable, and metacharacter text is preserved as literal caller-owned input to a permitted direct executable and is never expanded by KeptNear. Local help documents this boundary and reserves complete plaintext export for the interactive App. These first-party prohibitions are not general data-loss prevention: an authorized child or HTTP service can encode, transform, retain, or transmit delivered material. An actual `keptnear get` binary smoke test exited 2 without reflecting its private marker. Validation used no UI automation and intentionally accessed no live user Keychain, user Vault, or installed Broker. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, Vault doctor readiness, and local quality gates | Passed | Local task 10.6 evidence: 463 Rust tests, including 10 shared client, 21 MCP adapter, 236 Broker, 30 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 214 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, privacy, compatibility-disclosure, MCP setup, and public-source gates. `keptnear vault doctor [--json] <vault-path>` now exposes the existing read-only diagnostic under the public brand while legacy `psw doctor` syntax and the `psw` package default remain compatible. The namespaced action is separate from the seven Broker-connected machine command families, accepts no pairing profile, loads no Consumer Keychain identity, contacts neither the Broker nor a sync provider, and does not unlock or decrypt records. Malformed arguments use the fixed non-reflective parse error and Debug output omits the path. The readiness gate runs both real binaries against current v2/v2, incomplete, and unsupported future-format Vaults, confirms equivalent JSON and expected nonzero statuses, and scans text and JSON for the full Vault path plus seeded titles, credentials, and notes. Text differs only by the public `KeptNear vault doctor` title. Reports can include the unencrypted Vault display name from `vault.json`, so users must still review them before sharing. The first sandboxed full run failed only because Unix socket bind was denied; the required unsandboxed rerun passed all 236 Broker tests. Validation used no UI automation and no live user Keychain, installed Broker, or user Vault. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, CLI lifecycle regressions, and local quality gates | Passed | Local task 10.7 evidence: 468 Rust tests, including 11 shared client, 21 MCP adapter, 237 Broker, 33 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 214 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, 33 strict OpenSpec items, Vault format and doctor readiness, privacy, compatibility-disclosure, MCP setup, and public-source gates. Completed non-process commands exit 0, pairing-pending and fixed execution failures exit 1, invalid syntax remains 2, and completed `run` commands write one schema-version-one JSON result before propagating a numeric child status from 0 through 255; signal-based, missing, negative, or out-of-range status maps to 1. The shared client rejects mismatched request identity, protocol revision, capability catalog, and capability version, while the CLI also rejects operation-mismatched typed results with fixed non-reflective protocol-failure output. The macOS accepted-connection cancellation probe uses zero-timeout `poll`, detects hangup even when a queued byte remains readable, and does not consume that byte. Closing the CLI socket activates the existing process cancellation path, closes secret input, kills the directly launched child, and waits for it to be reaped; the regression completes in under one second and makes no claim about independently surviving descendants. Seeded request-side query, description, URL, header, body-path, body-content, process-argument, environment, and working-directory markers are excluded from CLI success, Broker-error, and protocol-error projections. This is request-reflection coverage, not task 6.6's later real Secret Field scan across Broker, MCP, CLI, and App adapters. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, CLI usage documentation gate, and local quality gates | Passed | Local task 10.8 evidence: 468 Rust tests, including 11 shared client, 21 MCP adapter, 237 Broker, 33 CLI, 125 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 216 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. `docs/cli-usage.md` now documents source-level human shell, script, and Agent subprocess use while explicitly retaining the current developer-preview and unshipped-Broker limitation. Human examples use synthetic canonical stable IDs, non-secret context, and only access, HTTP, process, Grant status, and revocation operations; they provide no raw-secret command, credential value, shell substitution, secret-bearing environment assignment, project persistence, or command-interpreter wrapper. Agent guidance distinguishes native MCP from CLI subprocess integration without changing the Broker trust model, requires one explicit profile boundary and the versioned JSON result, stops for local pairing or access approval, and requests an operation rather than a credential value. It states that caller instructions are advisory rather than KeptNear-enforced Agent policy and that the local user remains the approval authority. Output guidance treats base64 as reversible and authorized service or child output as untrusted and potentially sensitive. `script/verify_cli_usage_docs.sh` is part of `scripts/check.sh`, requires the availability, human, Agent, structured-output, compatibility, README, and build boundaries, and rejects common plaintext retrieval examples. Task 6.6's real Secret Field scan across Broker, MCP, CLI, and App remains separate. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, typed import regressions, and local quality gates | Passed | Local task 11.1 evidence: 470 Rust tests, including 11 shared client, 21 MCP adapter, 237 Broker, 33 CLI, 127 Core, 8 golden-vector, 3 property-hardening, and 30 FFI tests, plus 229 macOS tests, 216 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. Bitwarden JSON and generic CSV parsers now produce the extensible typed Credential model directly. Current-format commit allocates fresh local Credential, Secret Field, and revision identities and ignores source-provider IDs; preview writes no imported record. A focused regression verifies direct typed roles and provider-neutral kinds for login password, TOTP seed, secure-note body, card number, and verification code, proves persisted identities remain stable, and proves a repeated keep-duplicates import receives a disjoint set of three Credential and five Secret Field identities. Duplicate discovery now reads authenticated typed heads and compares only normalized non-secret title, template category, and login username plus first URL. A valid custom API Token that cannot convert to the frozen v1 model no longer blocks preview or commit. The bounded v1 import path remains a compatibility conversion for supported templates. Plaintext source-file risk is unchanged, typed export remains task 11.2, and real Secret Field adapter scans remain task 6.6. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, typed plaintext export regressions, and local quality gates | Passed | Local task 11.2 evidence: 474 Rust tests, including 11 shared client, 21 MCP adapter, 237 Broker, 33 CLI, 130 Core, 8 golden-vector, 3 property-hardening, and 31 FFI tests, plus 229 macOS tests, 217 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. The macOS human export now defaults to documented `keptnear-json` version 1. The Rust core builds an authenticated typed snapshot and preserves open template IDs, ordered fields, optional labels, provider-neutral secret kinds, active or archived status, tags, favorite state, and arbitrary secret bytes through reversible Base64. Source Vault, Credential, and Secret Field IDs are provenance only and carry no Consumer, Access Rule, Use Grant, or authorization. Deleted Credentials are absent; authenticated conflicts and rejected encrypted records are structured omissions rather than arbitrary or silently dropped data. The Bitwarden compatibility serializer skips a whole Credential when its template or any field cannot be represented, preventing partial secret-bearing output, and reports additional tag loss. Core, FFI, and macOS regressions cover binary secret preservation, source relationships, archived status, conflict omissions, compatibility losses, default format transport, and localized omission presentation. A synthetic fixture documents the plaintext schema. Base64 is not encryption, the exported file remains plaintext-equivalent, a KeptNear plaintext importer is not implemented, and tasks 11.3, 11.4, and 6.6 retain the machine-interface, device-state exclusion, and cross-adapter secret-marker gates. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, interactive plaintext-export boundary regressions, and local quality gates | Passed | Local task 11.3 evidence: 476 Rust tests, including 11 shared client, 21 MCP adapter, 238 Broker, 33 CLI, 131 Core, 8 golden-vector, 3 property-hardening, and 31 FFI tests, plus 229 macOS tests, 217 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. Complete plaintext export now requires the current master password in the App confirmation. FFI rejects an export command that omits it, and Core reopens the current key envelope and verifies that the unwrapped Vault Key matches the active session before snapshot construction or destination writing. Wrong-password regressions prove no destination is created and request Debug plus FFI error output exclude the password marker. The App clears its transient password binding after confirmation, cancellation, or lock; English, Simplified Chinese, and Japanese prompts describe reauthentication. Broker v1 remains a closed six-capability enum with no export variant, MCP advertises exactly the same six tools, and the CLI rejects whole-Vault, Credential, Secret, and plaintext export forms before dispatch, so Consumers, Access Rules, and Use Grants cannot authorize complete export. Swift and JSON bridge strings do not claim deterministic memory zeroization, the resulting file remains plaintext-equivalent, task 11.4 retains device-state exclusion evidence, and task 6.6 retains the cross-adapter real-secret scan. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, portable-boundary regression, and local quality gates | Passed | Local task 11.4 evidence: 477 Rust tests, including 11 shared client, 21 MCP adapter, 239 Broker, 33 CLI, 131 Core, 8 golden-vector, 3 property-hardening, and 31 FFI tests, plus 229 macOS tests, 217 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. Encrypted backup and restore use an exact portable root allowlist of `vault.json`, `keys.enc`, optional `recovery.enc`, `items/`, `attachments/`, and `tombstones/`; they exclude arbitrary root entries and `local_unlock.enc`. Plaintext export has a closed six-field top-level credential snapshot. A cross-module regression creates a real SQLCipher device database containing a Consumer, Access Rule, Use Grant, Usage Profile, audit event, and paused machine-access setting alongside a current-format Vault and local-unlock envelope. It verifies the exact backup and export shapes, scans both outputs for the actual trust-state IDs, label, database filename, and table markers, and then reloads every record and setting to prove output creation did not mutate device trust. Public documentation states that `~/.keptnear`, Keychain device and Consumer identities, approvals, audit, settings, runtime state, and convenience unlock do not transfer, and restore or import does not recreate authorization. The original Vault may still contain an authenticated `local_unlock.enc` for same-device use or encrypted file sync, but portable backup deliberately omits it. Task 6.6 retains the cross-adapter real-secret output scan. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, duplicate-Vault identity regressions, and local quality gates | Passed | Local task 11.5 evidence: 480 Rust tests, including 11 shared client, 21 MCP adapter, 241 Broker, 33 CLI, 131 Core, 8 golden-vector, 3 property-hardening, and 32 FFI tests, plus 231 macOS tests, 217 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. Broker registration now canonicalizes and reopens the selected path with the human-authenticated `vault_id` as an expected identity; FFI uses this operation after master-password, local-material, and recovery unlock instead of trusting an existing ID snapshot. A second path with the same ID or a tracked path whose identity changed is rejected without insertion or replacement. The App-to-Broker bridge locks an older machine session and makes its grants unusable while preserving the separate human Core unlock, then returns only `apps_tools_vault_path_conflict: true`. Swift decodes that field with a legacy false default, suppresses the Apps & Tools snapshot query, clears authorization inventory, displays localized English, Simplified Chinese, or Japanese warning text, and clears the conflict state on lock. Broker tests prove the original path remains authoritative and neither full path appears in Display, Debug, snapshots, or Consumer output; the dispatcher maps both internal variants to generic `operation-failed`, and FFI exposes no path value. Detection occurs when a path is presented to one Broker process and is not a filesystem-wide duplicate scan; the current alpha may require reopening only the intended copy after `Copy to Sync`. The local security evidence verifier passed through the accepted experimental-risk path; external review remains incomplete and production use remains not recommended. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, ancestry-aware merge regressions, and local quality gates | Passed | Local task 11.6 evidence: 486 Rust tests, including 11 shared client, 21 MCP adapter, 241 Broker, 33 CLI, 137 Core, 8 golden-vector, 3 property-hardening, and 32 FFI tests, plus 231 macOS tests, 217 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. Current-format refresh writes a fresh encrypted two-parent revision only for exactly two logical Credential heads with one unique known authenticated semantic base and no rejected records. Title, template ID, tags, and favorite are separate components; each Secret Field is indivisible by immutable field ID; all text fields are one aggregate component because they lack stable identities. Focused regressions prove independent title, tag, and distinct Secret Field changes merge; same-Secret-Field, concurrent text, missing-base, and ambiguous-base changes preserve both heads; repeat refresh is idempotent; and independently generated equal-content merges converge logically only when lifecycle and exact parent IDs also match. Revision and Device IDs remain random, all encrypted source records remain immutable, and any content or parent difference remains a conflict. The implementation adds no FFI, CLI, MCP, Consumer, diagnostics, or plaintext secret projection. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, non-destructive conflict regressions, and local quality gates | Passed | Local task 11.7 evidence: 490 Rust tests, including 11 shared client, 21 MCP adapter, 241 Broker, 33 CLI, 141 Core, 8 golden-vector, 3 property-hardening, and 32 FFI tests, plus 231 macOS tests, 217 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. Unsafe current-format refresh preserves byte-identical encrypted directory snapshots and every authenticated head for divergent same-Secret-Field values, equal ordinary single-parent Secret Field replacements, same-title changes, concurrent text edits, missing bases, ambiguous bases, and three concurrent heads. Delete-edit coverage proves the active edit remains under `items/`, the deletion remains under `tombstones/`, both authenticate after refresh, and the Credential remains visible as conflicted. Multi-parent merge equivalence cannot collapse ordinary single-parent conflicts. Manual resolution remains append-only and names all resolved heads without erasing source history. This preservation path adds no secret, path, diagnostics, or Consumer projection. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, typed conflict regressions, and local quality gates | Passed | Local task 11.8 evidence: 495 Rust tests, including 11 shared client, 21 MCP adapter, 241 Broker, 33 CLI, 144 Core, 8 golden-vector, 3 property-hardening, and 34 FFI tests, plus 232 macOS tests, 217 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. Current-format conflict candidates operate directly on authenticated typed Credential heads and include deletion revisions. Text fields are available only to the unlocked human view; Secret Fields project only role, optional label, stable field identity, kind, value presence, and changed state. Focused regressions scan Core Debug and FFI JSON for seeded API-token values, reject stale non-head selection without writing, preserve exact source item and tombstone records, retain all resolved heads as parents, report reordered text-field layouts, support explicit deletion resolution, and prove the quick resolver never chooses deletion implicitly. Existing explicit safe non-secret merge remains only for losslessly mapped non-deleted built-in candidates. Resolution cleans removed local field authorization before appending the selected complete Credential and lifecycle. Validation used no UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, two-device vault-format readiness, and local quality gates | Passed | Local task 11.9 evidence: 500 Rust tests, including 11 shared client, 21 MCP adapter, 241 Broker, 33 CLI, 144 Core, 8 golden-vector, 3 property-hardening, 5 two-device sync, and 34 FFI tests, plus 232 macOS tests, 218 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. A dedicated public-API integration matrix migrates the frozen v1 fixture, creates portable encrypted copies at distinct local paths, and exchanges only encrypted item and tombstone revision files. It proves migrated vault, Credential, Revision, and Secret Field identity continuity; convergence of independent Credential edits; byte-preserving same-Secret-Field and delete-edit conflicts; explicit active and deleted outcomes; and a temporary missing-parent conflict clearing after authenticated parent arrival without an automatic resolution write. The Broker duplicate-path regression now uses the formal portable-backup operation and rejects a second canonical path with the same `vault_id` without returning either path. The readiness script requires and runs this matrix plus the focused Broker check. These local file copies model revision arrival but do not verify any sync provider's upload, deletion, or ordering behavior. Validation used no network provider, UI automation, live user Keychain, user Vault, installed Broker, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh` | Passed | Local tasks 12.1 and 12.2 repository baseline: 509 Rust tests, 232 macOS tests, 222 public-source candidates, 116 dependency licenses, workspace Clippy with all targets and features plus warnings denied, and 33 strict OpenSpec items. This is not an external security review. |
| 2026-07-30 | `script/package_macos_alpha.sh` | Passed | Generated the unsigned Apple Silicon `local-test` DMG containing the App, Broker, MCP adapter, CLI, FFI library, embedded App metadata, and one closed protocol manifest. This baseline does not approve the dedicated unsigned-publication profile. |
| 2026-07-30 | `script/verify_macos_alpha_artifact.sh` | Passed | Verified regenerated DMG SHA-256 `ed92a63052b0886c50c22ae4923ec71bb4440069616e223c0c915af92a9be6d7`, byte-identical adjacent and in-DMG protocol manifests, canonical post-signing component hashes, executable presence, arm64 architecture, fixed installation paths, and `keptnear.broker/1.0`. The artifact remains dirty-worktree `local-test` evidence and is not publishable. |
| 2026-07-30 | `script/verify_public_source_tree.sh` | Passed | Verified 225 public-source candidates exclude local AI context, real vaults, plaintext exports, credentials, and build products. |
| 2026-07-30 | protocol-manifest and first-run regressions | Passed | Local tasks 12.1 and 12.2 focused evidence rejects unknown manifest fields, missing components, altered paths, uppercase or mismatched hashes, and metadata disagreement. Broker initialization occurs only when both device-key and all managed-state entries are absent; key-only, database-only, and sidecar-only authority is preserved and rejected rather than replaced. Bundling the Broker does not activate a long-running service or complete the App/MCP/CLI machine-access lifecycle. Validation used no UI automation, live user Keychain, user Vault, normal Broker service execution, or Codex Security. This is not an external security review. |
| 2026-07-30 | `scripts/check.sh`, release-profile contract, and review-policy gates | Passed | Local tasks 12.3 and 12.4 evidence: 509 Rust tests, 232 macOS tests, 225 public-source candidates, 116 dependency licenses, all-target all-feature Clippy with warnings denied, and 33 strict OpenSpec items. Separate executable entry points now govern source preview, unsigned experimental Apple Silicon DMG, and optional signed distribution. Source readiness builds no artifact; `unsigned-experimental` forbids signing and notarization, requires clean source plus AR-002, and has distinct manifest checks; signed readiness retains Developer ID, hardened runtime, notarization, stapling, Gatekeeper, and signed-install gates. All three review policies passed while reporting `Audit status: unaudited`; source and unsigned profiles do not treat absent external review as a blocker, and every profile keeps production use not recommended. Contract regressions reject an unknown policy profile, a signing identity supplied to unsigned packaging, dirty-worktree unsigned packaging, and dirty-worktree source readiness. The default dirty-worktree `local-test` DMG was regenerated and strictly verified without acquiring distribution status. No clean-source unsigned publication artifact, signed artifact, Apple notary request, UI automation, live user Keychain, user Vault, normal Broker service, or Codex Security was used. This is not an external security review. |
| 2026-07-31 | `scripts/check.sh`, public capability claims, and public documentation set | Passed | Local tasks 12.5 and 12.6 evidence: 509 Rust tests, 232 macOS tests, 228 public-source candidates, 116 dependency licenses, all-target all-feature Clippy with warnings denied, and 33 strict OpenSpec items. Public copy now uses four evidence-bound labels: available in the macOS source build, implemented and tested in source, bundled but not activated, and not shipped. The claim gate binds the exact six MCP capabilities and packaged Broker/MCP/CLI paths while rejecting production, external-audit, automatic-sync, and end-user machine-interface overclaims. The documentation gate verifies build, unsigned installation, recovery, privacy, local sync, import/export, troubleshooting, and README navigation; it also rejects stale recovery and document-registration statements. The App descriptor now says Local Password & Token Manager in English, Simplified Chinese, and Japanese, but installing the App still does not activate a Broker service or ship end-user MCP/CLI access. No packaging regeneration, UI automation, live user Keychain, user Vault, normal Broker service execution, network provider, or Codex Security was used. This is not an external security review. |
| 2026-07-31 | `scripts/check.sh`, repository-history secret scan, and GPLv3 compatibility gate | Passed | Local task 12.7 evidence: 509 Rust tests, 232 macOS tests, 229 public-source candidates, 367 unique reachable historical Git blobs, 116 registry dependencies, all-target all-feature Clippy with warnings denied, and 33 strict OpenSpec items. The current-tree gate excludes OpenSpec, EchoPath, Codex configuration, real Vaults, plaintext exports, signing material, and build products. A separate gate scans current candidates and all reachable history for credential-bearing filenames, known private-key and provider-token formats, private AI-development paths, symbolic links, and the current developer home path. License verification requires the complete GPLv3 text, `GPL-3.0-only` workspace metadata, inherited author and repository metadata, disabled crate publication, registry-only dependency sources, an exact reviewed expression allowlist, no MPL secondary-license opt-out, the bundled SQLCipher notice, and no Swift package or binary-target dependency. The current main and nested OpenSpec worktrees remain intentionally dirty during implementation, so strict final-revision source publication readiness has not been claimed. No UI automation, live user Keychain, user Vault, normal Broker service, network provider, or Codex Security was used. This is not an external security review or legal advice. |
| 2026-07-31 | `scripts/check.sh`, `script/verify_cross_adapter_secret_markers.sh`, local security review, and `script/verify_local_alpha_readiness.sh` | Passed | Local tasks 6.6, 12.9, and 12.10 evidence: 522 Rust tests and 234 Swift tests passed, with 232 public-source candidates, 367 reachable historical Git blobs, 116 registry dependency licenses, all-target all-feature Clippy with warnings denied, frozen-format and two-device readiness, vault Doctor, macOS security-state suites, Release-config packaging, DMG integrity, component-manifest, arm64, and Launch Services checks. The seeded-marker gate scans Broker audit output, every MCP tool error path, CLI success and error output, and App diagnostics. The local review closed seven findings across fixed KDF costs and canonical envelopes, bounded no-follow vault/import parsing, owner-only no-follow plaintext export, bundle-only release FFI loading, TLS dependency advisories, CLI Doctor, and closed FFI command schemas. `rustls 0.23.43` selects `rustls-webpki 0.103.13`; `cargo-audit 0.22.2 --no-fetch --stale` reported zero known vulnerabilities against the available 1,169-advisory snapshot, while an online database refresh did not complete and the snapshot exposed no last-update timestamp. The exact unsigned dirty-worktree `local-test` DMG has SHA-256 `b3393d84256a329a79e2b5facf961e0284b3c72e6b746f85e3eea0a2620f2ab6`; it remains non-distributable and still requires human acceptance. Remaining unaudited, unsigned, SQLCipher, provider-sync, memory-zeroization, Consumer-misuse, duplicate-vault, machine-component activation, and manual-update limitations are recorded in `docs/release-readiness.md`. Validation used no UI automation, live user Keychain, user Vault, installed Broker, network provider, or Codex Security. This is a maintainer-directed local review, not an external audit, production approval, or legal advice. |
| 2026-07-31 | SQLCipher 4.10 source-bound distribution revalidation | Passed | Upgraded the reviewed toolchain to Rust/Cargo 1.93.0, `rusqlite` to 0.39.0, `libsqlite3-sys` to 0.37.0, and bundled SQLCipher to 4.10.0. The exact distribution runner passed its compiler identity probe, 23 encrypted state-store tests, five fail-closed runtime tests for ciphertext corruption, wrong or missing device keys, unsafe permissions, and missing database state, 125 dependency-license checks, and all-target workspace Clippy with warnings denied. The runner used a clean process environment, offline locked dependencies, reviewed Apple tools, and no user/workspace Cargo configuration. This is local maintainer evidence, not an external audit or production approval. |
| 2026-07-31 | `scripts/check.sh` and `v0.1.0-prealpha.2` release preparation | Passed | The complete candidate passed 523 Rust tests, 234 Swift tests, seeded-secret output checks, 125 dependency-license checks, release-profile and public-claim contracts, 236 public-source candidates, and 349 reachable historical Git blobs. Real macOS Unix socket tests ran outside the restricted sandbox. The candidate remains externally unaudited, unsigned, and unsuitable for production credentials; no Codex Security service was used. |
| 2026-08-10 | `script/verify_macos_service_management_probe.sh` on macOS 26.5.2 arm64 with Xcode 26.6 | Partial | The build-only probe produced arm64 unsigned and ad-hoc bundles. Real `SMAppService` registration rejected the unsigned containing App with `SMAppServiceErrorDomain` code 3 (`kSMErrorInvalidSignature`). The ad-hoc probe registered as enabled, launched its bundled Agent, followed an App move through `BundleProgram`, unregistered, replaced the service plist generation, re-signed, re-registered, and launched generation 2. Cleanup left no matching launchd job or Agent process. No valid Developer ID identity was installed, and the `requires-approval` state was not observed, so signed-distribution and system-approval acceptance remain incomplete. No product Broker, user Vault, credential, Consumer state, network provider, or Codex Security service was used. The current DMG remains bundled but not activated. |
| 2026-08-12 | `scripts/check.sh`, controller-boundary regressions, GitHub Codex Review, and SQLCipher source revalidation | Passed | Local OpenSpec tasks 1.5 and 2.1-2.8 evidence: 582 Rust tests, 235 Swift tests, 264 public-source candidates, 602 reachable historical Git blobs, 125 dependency-license checks, all-target all-feature Clippy with warnings denied, and a refreshed source-bound SQLCipher 4.10 receipt. Coverage includes strict versioned fixtures, non-exportable Controller key authority, fail-closed key/state mismatch handling, single-use Ed25519 challenges, exact Human Control schemas and dispatch, owner-only singleton process locking, and path-free readiness categories. Forty-five automated PR review findings were fixed with regressions for hard-linked and replaceable diagnostic lock files, shared App-and-service runtime ownership, pre-migration database integrity, bounded malformed and authority-mismatch authentication failures, Broker-derived challenge-only fields, executable-compatible LaunchAgent arguments, protocol-consistent grant-revocation responses, bounded audit pages and exports, pairing identity evidence, the combined pending-queue bound, bounded authorization and Consumer-detail collections, Broker-generated Controller sessions, shared credential-review metadata byte bounds, exact audit-export request limits, preserved Access Rule lifetimes, ordered retryable Controller-authority removal during confirmed device-data clearing, exact unlock Vault and authorization capability bindings, role-and-schema-bound negotiation, repair compatibility checks before runtime quiescence, non-Wire-only Controller seed creation after explicit enablement, exact existing-field approval scope, fixed denial decisions, echoed lease identities, fixed Consumer revocation scope and shutdown reason, exact echoed Controller identity binding in authentication proofs, wire-constructible Controller proofs without Broker seed access, strict operation-specific wire value decoding, canonical and decoded-size-bounded Base64, redacted and zeroizing wire envelopes and owning frame payloads, Broker-issued connection-local bounded single-use audit-clear tickets, exact monotonic Controller-lease expiry and renewal, preservation of dispatcher-owned protocol-incompatible repair responses, structurally valid incompatible Hello offers reaching negotiation, typed audit-cursor reconstruction, exhaustive Wire-to-typed reconstruction for all 29 frozen operations, dispatcher-wide invalidation of current and new controller connections after successful repair or shutdown quiescence, exact negotiated frame, collection, audit, and input-text limits in the typed Hello projection, direct read-only projection of the non-secret Controller identity and public key required to encode a challenge response without parsing its signed transcript, and readiness projection of the exact protocol and schema retained by the authenticated connection rather than the running Broker's current version. Readiness Vault state is likewise bounded at the Human Control projection. The Broker remains bundled but not activated; App client integration, lease-loss-driven Vault locking, entitlement installation, and service activation remain later tasks. Validation used no UI automation, live user Keychain, user Vault, installed product Broker, network provider, or Codex Security. This is local maintainer evidence, not an external audit or production approval. |

## Release Decision

External review has not approved KeptNear. The maintainer accepted-risk records
approve only the policy basis for bounded experimental profiles. Overall
artifact readiness still requires every non-review gate applicable to the
selected profile. Signed-install verification applies only to the signed
profile; the unsigned profile requires its own verifier and disclosures.

- External review completed: No
- Critical findings fixed or explicitly accepted: No
- High findings fixed or explicitly accepted: No
- Medium findings fixed, mitigated, or tracked: No
- Validation after review-driven changes passed: No
- Security model or readiness claims updated after review: No
- Public alpha decision: Not approved
- Signed public alpha artifact decision: Not approved
- Unsigned experimental DMG artifact decision: Approved
- Experimental pre-release risk accepted: Yes
- Accepted risk ID: AR-001
- Unsigned accepted risk ID: AR-002
- Risk acceptance owner: Chase Chou
- Risk acceptance date: 2026-07-24
- Unsigned risk acceptance date: 2026-07-30
- Accepted release scope: v0.1.x pre-alpha macOS 13+ Apple Silicon DMG
- Unsigned accepted release scope: v0.1.x pre-alpha macOS 13+ Apple Silicon DMG
- Public alpha security decision: Approved for experimental pre-release
- Unsigned experimental security-policy decision: Approved for the bounded unsigned profile
- External review required before source or unsigned experimental publication: No
- External review required before production use: Yes
- External review required before production-use recommendation: Yes
- Production-use recommendation: Not recommended

## Maintenance Rules

- Do not mark review complete without a reviewer report or tracked finding list.
- Do not mark public alpha approved if Critical or High findings are open.
- Do not accept a risk without the required accepted-risk fields.
- Do not use `AR-001` to bypass signing, notarization, signed-install
  verification, user-facing warnings, or the accepted signed release scope.
- Do not use `AR-002` to imply signing, notarization, Apple identity
  verification, external audit, or production suitability. Require the
  dedicated unsigned profile, verifier, checksum, manifest, installation
  guidance, and user-facing warnings.
- Keep production-use recommendation separate from public alpha readiness.
- Keep `script/verify_sqlcipher_distribution_gate.sh` independent from these
  Markdown decisions. A binary artifact cannot pass until its reviewed
  dependency mapping and source-bound machine-readable revalidation receipt
  pass that executable gate.
- Run `script/verify_security_review_evidence.sh` before an experimental
  pre-release. Strict mode passes only when either external review evidence is
  complete or the maintainer accepted-risk path is complete. `--allow-missing`
  remains a report mode.
- Replace explicit missing-state rows with external review evidence,
  finding-disposition records, or documented accepted risks before approving a
  broader release.
